stages:
  - build_and_push
  - deploy

variables:
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  IMAGE_NAME: quay.io/sambhajimene/mk
  FULL_IMAGE: quay.io/sambhajimene/mk:$CI_COMMIT_SHORT_SHA
  OPENSHIFT_PROJECT: sambhajimene-dev

# =====================
# BUILD & PUSH
# =====================
build_and_push:
  stage: build_and_push
  image: quay.io/buildah/stable:latest
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "**/*.py"
        - Dockerfile
        - requirements.txt
    - when: never
  script:
    - echo "Building image $FULL_IMAGE"
    - buildah bud -t $FULL_IMAGE .
    - echo "Logging in to Quay.io"
    - buildah login -u $QUAY_USER -p $QUAY_PASSWORD quay.io
    - echo "Pushing image $FULL_IMAGE to Quay.io"
    - buildah push $FULL_IMAGE docker://$FULL_IMAGE

# ---------------- DEPLOY ----------------
deploy:
  stage: deploy
  image: quay.io/openshift/origin-cli:latest
  script:
    - echo "🔐 Login to OpenShift"
    - oc login "$OPENSHIFT_SERVER" --token="$OPENSHIFT_TOKEN" --insecure-skip-tls-verify=true

    - echo "📦 Select project"
    - oc project "$OPENSHIFT_PROJECT"

    - echo "🚀 Deploying / Updating app"
    - |
      oc get deployment mk >/dev/null 2>&1 || \
      oc create deployment mk --image=$IMAGE_NAME

    - oc set image deployment/mk mk=$IMAGE_NAME
    - oc rollout status deployment/mk

    # ---------------------
    # SERVICE
    # ---------------------
    - echo "🔌 Creating service (if not exists)"
    - |
      oc get svc $APP_NAME >/dev/null 2>&1 || \
      oc expose deployment $APP_NAME --port=$APP_PORT --name=$APP_NAME

    # ---------------------
    # ROUTE
    # ---------------------
    - echo "🌐 Creating route (if not exists)"
    - |
      oc get route $APP_NAME >/dev/null 2>&1 || \
      oc expose svc $APP_NAME

    # ---------------------
    # DISPLAY OUTPUT
    # ---------------------
    - echo "✅ Deployment successful"
    - echo "🌐 Application URL:"
    - oc get route $APP_NAME -o jsonpath='http://{.spec.host}'
    - echo ""
  only:
    - main

