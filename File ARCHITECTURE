fyers_client.py → APIv3 compatible, token-based

option_chain.py → dynamic CE/PE strike fetch ±5 strikes

seller_logic.py → seller bias calculation

confidence.py → confidence scoring

alerts.py → email + repeated alert prevention

main.py → fully Streamlit + OpenShift + auto-refresh + sidebar + LOC + alerts

config.py